# Copy to control/.env, then paste your key after the equals sign.
# control/.env is ignored by Git and must stay local.
OPENAI_API_KEY=

# Optional model overrides.
# INTENT_MODEL=gpt-5.6-sol
# EXPLAIN_MODEL=gpt-5.6-luna
