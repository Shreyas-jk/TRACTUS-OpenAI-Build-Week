# Rust (Cargo.lock stays tracked: workspace ships binaries)
/target

# Secrets and local caches (OPENAI_API_KEY lives in .env, never committed)
.env
__pycache__/
.venv/

# Local-only planning material (never in the public submission repo)
/private/

# OS
.DS_Store

# Machine-local Codex hook path (generated by `tractus init`)
.codex/hooks.json

# Local, user-approved Tractus contract history
.tractus/
