# Rust (Cargo.lock stays tracked: workspace ships binaries)
/target

# Python control plane
__pycache__/
*.pyc
.venv/
.env

# Local-only planning material (never in the public submission repo)
/private/

# OS
.DS_Store

# Machine-local Codex hook path (generated by scripts/install-hook.sh)
.codex/hooks.json
