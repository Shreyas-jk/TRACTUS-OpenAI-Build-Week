- One module per prompt. Never ask for two subsystems in one message.
- Paste compiler and test errors verbatim; do not paraphrase them.
- Hard cap: 3 fix iterations per failure, then stop Terra and do the groundwork manually.
- Keep DESIGN.md and CHAOS_CORE.md pinned in the session context (drag the repo into the side
  panel once, at the start) so cached-input pricing applies.
- Every prompt ends with an executable acceptance check. Do not move on while red.
